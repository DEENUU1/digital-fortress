from processor.split import split_files
from embedding.pinecone import save_to_pinecone

# def process_uploaded_file():
#
#     path = "/files/ads.txt"
#     chunks = split_files(path)
#     save_to_pinecone(chunks, pinecone_index, openai_key)
